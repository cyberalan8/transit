.routes
  %span
    What Bus Route:
  = select("route", "number", BusRoute.all.collect {|b| [ format_bus_routes(b), b.number ] }, {:include_blank => 'Select One'})

.directions
  %span
    What Direction:
  = select_tag 'direction'

.stops
  %span
    Which Stop:
  = select_tag 'stop'

.find_my_bus
  = link_to('Find My Bus!', '', :id => 'find_my_bus', :class => 'disabled button')

.nav
  = link_to('<< Back', home_index_path)
